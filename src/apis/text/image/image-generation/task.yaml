summary: ''

default-model: "runwayml-sd-15"

inputs:
  prompt:
    type: str
    placeholder: "prompt"
    examples:
      - "The Eiffel tower landing on the moon, unreal engine."

  samples:
    type: int
    placeholder: "number of samples to return"
    default: 1
    examples:
      - 1

  steps:
    type: int
    placeholder: "number of steps to execute"
    default: 40
    examples:
      - 40

  seed:
    type: int
    placeholder: "seed to use"
    default: 396916372
    examples:
      - 396916372

output:
  name: image
  type: image
  example: ""
