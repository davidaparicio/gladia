summary: 'Format the input string as an clean adress using geopy'

default-model: "geopy-formatter"

inputs:
  address:
    type: str
    placeholder: "adress to format"
    examples:
      - "Taj Mahal, Agra, Uttar Pradesh 282001"

output:
  name: text
  type: text
  example: ""
